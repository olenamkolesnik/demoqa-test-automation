
# Environment
.env

# Claude Code local config — not published on the remote.
# Version-controlled separately in its own local-only git repo at .claude/.git
# (never pushed anywhere). Earlier versions of the previously-tracked subset
# are recoverable from this repo's history at bc725d1^.
.claude/

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
.vscode/settings.json
